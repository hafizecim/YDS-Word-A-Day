name: Gunluk YDS Kelime Botu

on:
  schedule:
    - cron: '0 5 * * *' # Her sabah 08:00 TSİ
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-all:
    runs-on: ubuntu-latest
    steps:
    - name: Depoyu Klonla
      run: |
        git clone https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git .

    - name: Kelimeyi Seç (Python)
      run: python3 update_word.py

    - name: Depo Açıklamasını Güncelle (Zorlu Yöntem)
      env:
        GH_TOKEN: ${{ secrets.STATUS_TOKEN }}
      run: |
        # Python'ın oluşturduğu kelime metnini oku
        DESC=$(cat repo_desc.txt)
        # GitHub CLI ile deponun 'About' kısmını doğrudan güncelle
        gh repo edit ${{ github.repository }} --description "$DESC"

    - name: YDS Deposuna Push Et
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "hafizecim"
        # Geçici text dosyasını commit etmeye gerek yok
        rm repo_desc.txt
        git add Gunun_Kelimesi.md
        git commit -m "Daily YDS Word Update: $(date +'%Y-%m-%d')" || exit 0
        git push

    - name: Profil Durum Balonunu Güncelle
      env:
        USER_TOKEN: ${{ secrets.STATUS_TOKEN }}
      run: |
        WORD=$(python3 -c "
        import json, random
        with open('words.json', 'r', encoding='utf-8') as f:
            w = random.choice(json.load(f))
        print(f\"YDS Today: {w['word']} ({w['meaning']})\")
        ")
        
        curl -X POST -H "Authorization: bearer $USER_TOKEN" \
          -d "{\"query\": \"mutation { changeUserStatus(input: { message: \\\"$WORD\\\", emoji: \\\":speech_balloon:\\\" }) { status { message } } }\"}" \
          https://api.github.com/graphql
